# Content Registry - Умный реестр контента с AI-аналитикой by Nestor Team

> Веб-приложение для управления и анализа контента из всех социальных сетей с интегрированным AI-ассистентом и дашбордами MWS Tables.
> Скринкаст: https://drive.google.com/file/d/1DlJblwH_qhtYlclU4gxBSQeONBvqdCl7/view?usp=sharing

## О команде

- Алдаг Алгюль - тимлид, разработчик, кайфует от кода в 4 утра
- Копытько Николай - продукт, отвечает за обоснование решения и проблемы
- Подлуцкая Юлия - фронтенд-разработчик, ждёт дизайнера
- Иванова Мария - дизайнер, и вы обязательно увидите красивые мокапы того, как это могло выглядеть, в презентации
- Архангельский Нестор - вайб-продукт, талисман команды

## 📑 Навигация по документации

- [Описание проекта](#описание-проекта)
- [Возможности](#возможности)
- [Структура репозитория](#структура-репозитория)
- [Установка и настройка](#установка-и-настройка)
- [Запуск проекта](#запуск-проекта)
- [Архитектура](#архитектура)
- [API документация](#api-документация)
- [Компоненты и страницы](#компоненты-и-страницы)
- [Настройка интеграций](#настройка-интеграций)
- [MWS Tables - Детальное руководство](#mws-tables---детальное-руководство)
- [Разработка](#разработка)
- [Решение проблем](#решение-проблем)
- [FAQ](#faq)

---

## Описание проекта

Content Registry - это комплексная платформа для управления и анализа контента из различных социальных сетей. Проект построен на основе MWS Tables как основного источника данных и включает в себя:

- **Единый дашборд** для всех социальных сетей
- **Детальная аналитика** с интерактивными графиками
- **AI-ассистент** для получения рекомендаций
- **Анализ тональности** комментариев в реальном времени
- **Интеграция с MWS Tables** для хранения и визуализации данных

### Технологический стек

**Frontend:**
- React 18.2.0
- React Router DOM 6.8.0
- Recharts 3.5.1 (графики)
- Lucide React (иконки)
- Vite 5.0.8 (сборка)
- Axios 1.6.0 (HTTP клиент)

**Backend:**
- Node.js
- Express 4.18.2
- Axios 1.6.0
- OpenRouter API (AI)
- MWS Tables API

---

## Возможности

### Основные функции

1. **Единый дашборд для всех социальных сетей**
   - VKontakte (VK)
   - Telegram
   - Instagram
   - YouTube
   - Сравнительная статистика
   - Фильтрация по сетям

2. **Детальная аналитика**
   - Динамика просмотров, лайков, комментариев
   - Распределение типов контента
   - Анализ тональности комментариев
   - Временные ряды
   - Выбор периода (7/30/90 дней, все время)
   - Фильтрация по социальным сетям

3. **AI-ассистент**
   - Анализ контента
   - Рекомендации по улучшению
   - Ответы на вопросы о метриках
   - Прогнозирование популярности

4. **Управление контентом**
   - Таблица всех материалов
   - Фильтрация по типу и тональности
   - Сортировка по метрикам
   - Экспорт в CSV

5. **Анализ тональности**
   - Анализ комментариев в реальном времени
   - История изменения тональности
   - Визуализация настроения аудитории

6. **MWS Tables интеграция**
   - Отображение дашбордов из MWS
   - Синхронизация данных
   - Виджеты и метрики

---

## Структура репозитория

```
MWS_Smart_Nestor_Team/
│
├── content-registry-backend/          # Backend приложение
│   ├── config/                         # Конфигурационные файлы
│   │   └── database.js                 # Настройки базы данных
│   │
│   ├── controllers/                    # Контроллеры (обработка запросов)
│   │   ├── aiController.js             # Контроллер для AI функций
│   │   ├── botController.js            # Контроллер для AI ассистента
│   │   ├── mwsController.js            # Контроллер для MWS Tables
│   │   ├── sentimentController.js      # Контроллер для анализа тональности
│   │   ├── socialNetworksController.js # Контроллер для социальных сетей
│   │   └── vkController.js             # Контроллер для VK API
│   │
│   ├── models/                         # Модели данных
│   │   └── Content.js                  # Модель контента
│   │
│   ├── routes/                          # API маршруты
│   │   ├── aiRoutes.js                 # Маршруты для AI
│   │   ├── botRoutes.js                # Маршруты для бота
│   │   ├── mwsRoutes.js                # Маршруты для MWS
│   │   ├── sentimentRoutes.js              # Маршруты для тональности
│   │   ├── socialNetworksRoutes.js     # Маршруты для социальных сетей
│   │   └── vkRoutes.js                 # Маршруты для VK
│   │
│   ├── services/                       # Бизнес-логика и интеграции
│   │   ├── aiService.js                # Сервис для работы с AI (OpenRouter)
│   │   ├── api.js                      # Общие API утилиты
│   │   ├── mwsService.js               # Сервис для работы с MWS Tables
│   │   ├── notificationService.js      # Сервис уведомлений
│   │   ├── socialNetworksService.js     # Сервис для социальных сетей
│   │   └── vkService.js                # Сервис для VK API
│   │
│   ├── utils/                          # Утилиты
│   │   ├── cronJobs.js                 # Планировщик задач
│   │   └── helpers.js                  # Вспомогательные функции
│   │
│   ├── .env                            # Переменные окружения (создать)
│   ├── .env.example                     # Пример переменных окружения
│   ├── package.json                    # Зависимости backend
│   ├── package-lock.json               # Зафиксированные версии
│   └── server.js                        # Точка входа сервера
│
├── content-registry-frontend/          # Frontend приложение
│   ├── public/                          # Статические файлы
│   │
│   ├── src/                            # Исходный код
│   │   ├── components/                 # React компоненты
│   │   │   ├── ChatInterface.jsx       # Интерфейс AI ассистента
│   │   │   ├── ContentTable.jsx         # Таблица контента
│   │   │   ├── LoadingSpinner.jsx       # Индикатор загрузки
│   │   │   ├── Message.jsx              # Компонент сообщения
│   │   │   ├── MWSDashboard.jsx         # Компонент MWS дашборда
│   │   │   ├── Navigation.jsx            # Навигационное меню
│   │   │   └── StatsCards.jsx          # Карточки статистики
│   │   │
│   │   ├── pages/                      # Страницы приложения
│   │   │   ├── Analytics.jsx            # Страница аналитики
│   │   │   ├── ChatBot.jsx              # Страница AI ассистента
│   │   │   ├── Content.jsx              # Страница управления контентом
│   │   │   ├── Dashboard.jsx            # Главная страница (дашборд)
│   │   │   └── SocialNetworks.jsx       # Страница всех социальных сетей
│   │   │
│   │   ├── services/                   # API клиенты и сервисы
│   │   │   └── api.js                   # API клиент (axios)
│   │   │
│   │   ├── styles/                      # Стили
│   │   │   └── index.css                # Основные стили приложения
│   │   │
│   │   ├── App.jsx                      # Главный компонент приложения
│   │   └── main.jsx                     # Точка входа React
│   │
│   ├── index.html                       # HTML шаблон
│   ├── package.json                     # Зависимости frontend
│   ├── package-lock.json                # Зафиксированные версии
│   └── vite.config.js                   # Конфигурация Vite
│
├── .gitignore                           # Игнорируемые файлы Git
├── README.md                            # Этот файл
└── SOCIAL_MEDIA_APIS.md                # Документация по API социальных сетей
```

### Детальное описание директорий

#### Backend

**`controllers/`** - Обработчики HTTP запросов
- Принимают запросы от клиента
- Валидируют данные
- Вызывают соответствующие сервисы
- Возвращают ответы

**`services/`** - Бизнес-логика
- Взаимодействие с внешними API
- Обработка данных
- Интеграции с MWS Tables, OpenRouter, VK и др.

**`routes/`** - Определение API endpoints
- RESTful маршруты
- Подключение контроллеров
- Middleware для аутентификации и валидации

**`models/`** - Модели данных
- Структура данных
- Валидация схем

**`utils/`** - Вспомогательные функции
- Cron jobs для автоматических задач
- Утилиты для обработки данных

#### Frontend

**`components/`** - Переиспользуемые компоненты
- UI компоненты
- Компоненты для работы с данными
- Компоненты для визуализации

**`pages/`** - Страницы приложения
- Каждая страница - отдельный роут
- Основная логика страницы

**`services/`** - API клиенты
- Axios конфигурация
- Методы для работы с API
- Обработка ошибок

**`styles/`** - Стилизация
- Глобальные стили
- Компонентные стили
- Цветовая схема (красно-бело-светло-серая)

---

## Установка и настройка

### Требования

- **Node.js** 18.0.0 или выше
- **npm** 9.0.0 или выше (или yarn)
- **Git** для клонирования репозитория

### Шаг 1: Клонирование репозитория

```bash
git clone <repository-url>
cd MWS_Smart_Nestor_Team
```

### Шаг 2: Установка зависимостей Backend

```bash
cd content-registry-backend
npm install
```

### Шаг 3: Настройка переменных окружения Backend

Создайте файл `.env` в директории `content-registry-backend/`:

```env
# Порт сервера
PORT=3000

# MWS Tables API
MWS_API_KEY=your_mws_api_key_here
MWS_APP_ID=your_mws_app_id_here
MWS_BASE_URL=https://api.mws.tables

# OpenRouter API (для AI ассистента)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# VK API (опционально)
VK_SERVICE_TOKEN=your_vk_service_token_here

# Telegram Bot API (опционально)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Instagram API (опционально)
INSTAGRAM_ACCESS_TOKEN=your_instagram_access_token_here

# YouTube API (опционально)
YOUTUBE_API_KEY=your_youtube_api_key_here

# Окружение
NODE_ENV=development
```

> **Примечание:** Для работы без реальных API ключей приложение использует мок-данные автоматически.

### Шаг 4: Установка зависимостей Frontend

```bash
cd ../content-registry-frontend
npm install
```

### Шаг 5: Настройка Frontend (опционально)

Если backend работает на другом порту или хосте, настройте прокси в `vite.config.js`:

```javascript
export default {
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true
      }
    }
  }
}
```

---

## Запуск проекта

### Режим разработки

**Терминал 1 - Backend:**
```bash
cd content-registry-backend
npm run dev
```

Backend будет доступен на `http://localhost:3000`

**Терминал 2 - Frontend:**
```bash
cd content-registry-frontend
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

### Режим production

**Backend:**
```bash
cd content-registry-backend
npm start
```

**Frontend:**
```bash
cd content-registry-frontend
npm run build
npm run preview
```

---

## Архитектура решения

### Общая архитектура

Проект построен по принципу клиент-серверной архитектуры с разделением на frontend и backend компоненты, интегрированные с MWS Tables как основным источником данных.

```
┌─────────────────────────────────────────────────────────────┐
│                    Пользовательский интерфейс              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ Dashboard│  │ Analytics │  │  Content │  │   Chat   │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│                                                             │
│                    React Frontend (Vite)                    │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ HTTP/REST API
                        │
┌───────────────────────▼─────────────────────────────────────┐
│                    Backend API Server                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │  Routes  │  │Controllers│  │ Services │  │   AI     │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│                                                             │
│              Node.js + Express.js                           │
└───────────────────────┬─────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
┌───────▼──────┐ ┌──────▼──────┐ ┌──────▼──────┐
│  MWS Tables  │ │ OpenRouter  │ │ Social APIs│
│     API      │ │     API     │ │  (VK, etc) │
└──────────────┘ └─────────────┘ └────────────┘
```

### Компоненты системы

#### 1. Frontend (React Application)

**Технологический стек:**
- **React 18** - UI библиотека
- **React Router DOM** - маршрутизация
- **Vite** - сборщик и dev-сервер
- **Recharts** - графики и визуализация
- **Lucide React** - иконки
- **Axios** - HTTP клиент

**Структура:**
```
content-registry-frontend/
├── src/
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── Navigation.jsx    # Навигационное меню
│   │   ├── ChatInterface.jsx # Интерфейс AI-ассистента
│   │   ├── ContentTable.jsx  # Таблица контента
│   │   └── Notifications.jsx # Компонент уведомлений
│   ├── pages/               # Страницы приложения
│   │   ├── Dashboard.jsx    # Главная страница
│   │   ├── Analytics.jsx    # Аналитика
│   │   ├── Content.jsx      # Управление контентом
│   │   ├── SocialNetworks.jsx # Все социальные сети
│   │   ├── ChatBot.jsx      # AI-ассистент
│   │   └── NotificationsPage.jsx # Страница уведомлений
│   ├── services/            # API клиенты
│   │   └── api.js           # MWS API клиент
│   ├── styles/              # Стили
│   │   └── index.css        # Глобальные стили
│   ├── App.jsx              # Корневой компонент
│   └── main.jsx             # Точка входа
```

**Особенности:**
- Модульная архитектура компонентов
- Централизованное управление состоянием через React hooks
- Адаптивный дизайн (responsive)
- Темная/светлая тема
- Оптимизация производительности через React.memo и useMemo

#### 2. Backend (Node.js API Server)

**Технологический стек:**
- **Node.js** - серверная платформа
- **Express.js** - веб-фреймворк
- **Axios** - HTTP клиент для внешних API
- **Dotenv** - управление переменными окружения
- **CORS** - обработка CORS запросов

**Структура:**
```
content-registry-backend/
├── controllers/             # Контроллеры (обработка запросов)
│   ├── botController.js     # AI-ассистент
│   └── analyticsController.js # Аналитика
├── services/                 # Бизнес-логика
│   ├── mwsService.js        # Интеграция с MWS Tables
│   ├── aiService.js         # AI сервис (OpenRouter)
│   └── socialNetworksService.js # Социальные сети
├── routes/                  # Маршруты API
│   └── index.js            # Определение endpoints
├── models/                  # Модели данных
├── server.js                # Точка входа сервера
└── .env                     # Переменные окружения
```

**API Endpoints:**
- `GET /api/analytics` - получение аналитики
- `GET /api/content` - получение контента
- `POST /api/bot/message` - отправка сообщения AI
- `GET /api/bot/status` - статус AI сервиса

#### 3. MWS Tables Integration

**Роль в архитектуре:**
- Единый источник данных (Single Source of Truth)
- Хранилище всех метрик и контента
- Встроенные дашборды и визуализация
- API для синхронизации данных

**Поток данных:**
```
Социальные сети → Backend Service → MWS Tables API → Таблица content_registry
                                                          ↓
Frontend ← Backend API ← MWS Tables API ← Дашборды MWS
```

#### 4. AI Service (OpenRouter)

**Интеграция:**
- Использует OpenRouter API для доступа к LLM моделям
- Fallback система для работы без API ключа
- Умные промпты для маркетинга и аналитики
- Контекстные ответы на основе данных MWS Tables

**Промпты для маркетинга:**
1. Креативы для рекламы с высокой конверсией (AIDA, PAS)
2. Вирусные креативы для соцсетей
3. Креативы на основе сторителлинга
4. Убедительный рекламный текст (психологические принципы)
5. Хук для увеличения вовлеченности
6. Уникальный маркетинговый угол
7. Видео-реклама с высокой конверсией

**Архитектура AI:**
```
User Question → BotController → AIService
                                      ↓
                            ┌─────────┴─────────┐
                            │                   │
                    OpenRouter API      Smart Fallback
                    (если есть ключ)    (всегда работает)
                            │                   │
                            └─────────┬─────────┘
                                      ↓
                            Context Data (MWS)
                                      ↓
                            Formatted Response
```

### Потоки данных

#### 1. Загрузка аналитики
```
Frontend → GET /api/analytics
              ↓
         Backend Controller
              ↓
         MWS Service.getContentAnalytics()
              ↓
         MWS Tables API / Mock Data
              ↓
         Processed Analytics
              ↓
         Frontend Display
```

#### 2. AI-ассистент
```
User Input → POST /api/bot/message
                ↓
           BotController.handleMessage()
                ↓
           getContextData() → MWS Service
                ↓
           AIService.generateBotResponse()
                ↓
      ┌─────────┴─────────┐
      │                   │
OpenRouter API    Smart Fallback
      │                   │
      └─────────┬─────────┘
                ↓
         Formatted Response
                ↓
         Frontend Display
```

#### 3. Синхронизация данных
```
Cron Job / Manual Trigger
         ↓
Social Networks API (VK, Telegram, etc.)
         ↓
Backend Service.syncNetworkData()
         ↓
MWS Service.saveToTable()
         ↓
MWS Tables API
         ↓
Data Stored in content_registry
```

### Безопасность

- **Переменные окружения** - все ключи API в `.env`
- **CORS** - настройка для безопасности
- **Валидация данных** - проверка входных данных
- **Обработка ошибок** - graceful error handling
- **Fallback системы** - работа без внешних API

### Масштабируемость

**Горизонтальное масштабирование:**
- Stateless backend (можно запускать несколько инстансов)
- Frontend как статические файлы (CDN)
- MWS Tables как внешний сервис

**Вертикальное масштабирование:**
- Оптимизация запросов к MWS Tables
- Кэширование данных
- Пагинация больших списков

### Развертывание

**Development:**
- Frontend: `npm run dev` (Vite dev server)
- Backend: `npm start` (Node.js)

**Production:**
- Frontend: сборка через `npm run build`, раздача статики
- Backend: PM2 или Docker контейнер
- MWS Tables: облачный сервис

### Интеграции

1. **MWS Tables** - основной источник данных
2. **OpenRouter** - AI модели (опционально)
3. **VK API** - данные из VKontakte
4. **Telegram API** - данные из Telegram
5. **Instagram API** - данные из Instagram (через парсинг)
6. **YouTube API** - данные из YouTube

### Frontend архитектура

```
React App (App.jsx)
    ↓
React Router (Routes)
    ↓
Pages (pages/*.jsx)
    ↓
Components (components/*.jsx)
    ↓
API Service (services/api.js)
    ↓
Backend API
```

### Поток данных

1. **Пользователь** взаимодействует с UI
2. **React компонент** вызывает метод из `api.js`
3. **Axios** отправляет HTTP запрос на backend
4. **Express** обрабатывает запрос через routes → controllers
5. **Controller** вызывает соответствующий service
6. **Service** взаимодействует с внешними API или MWS Tables
7. **Данные** возвращаются обратно через цепочку
8. **React** обновляет UI с новыми данными

---

## API документация

### Базовый URL

- **Development:** `http://localhost:3000/api`
- **Production:** `https://your-domain.com/api`

### MWS Tables API

#### Получить данные контента
```http
GET /api/mws/content
Query Parameters:
  - limit: number (default: 50)
  - offset: number (default: 0)
  - type: string (optional: 'post', 'video', 'image')
```

**Response:**
```json
{
  "success": true,
  "data": {
    "rows": [...],
    "total_count": 100
  },
  "pagination": {
    "total": 100,
    "limit": 50,
    "offset": 0
  }
}
```

#### Получить аналитику
```http
GET /api/mws/analytics
```

**Response:**
```json
{
  "success": true,
  "analytics": {
    "summary": {
      "totalPosts": 156,
      "totalViews": 125000,
      "totalLikes": 12450,
      "totalComments": 2345,
      "avgEngagement": 4.2
    },
    "typeStats": {...},
    "topPosts": [...],
    "sentiment": {
      "positive": 0.65,
      "neutral": 0.25,
      "negative": 0.10
    }
  }
}
```

#### Получить дашборды
```http
GET /api/mws/dashboards
```

### Social Networks API

#### Получить данные всех сетей
```http
GET /api/networks
```

#### Получить данные конкретной сети
```http
GET /api/networks/:networkId
Path Parameters:
  - networkId: 'vk' | 'telegram' | 'instagram' | 'youtube'
```

#### Сравнительная статистика
```http
GET /api/networks/comparison/stats
```

### AI & Bot API

#### Отправить сообщение AI ассистенту
```http
POST /api/bot/message
Body:
{
  "message": "Какие посты были самыми популярными?"
}
```

**Response:**
```json
{
  "success": true,
  "response": {
    "text": "Ответ AI...",
    "timestamp": "2025-01-01T00:00:00.000Z",
    "context_used": true
  }
}
```

### Sentiment API

#### Анализ тональности комментариев
```http
GET /api/sentiment/analyze
Query Parameters:
  - postId: string (optional)
  - network: string (optional)
```

#### История тональности
```http
GET /api/sentiment/history
Query Parameters:
  - postId: string (optional)
  - days: number (default: 30)
```

---

## Компоненты и страницы

### Страницы

#### Dashboard (`/`)
- Главная страница приложения
- Ключевые метрики
- Топ материалы
- Распределение типов контента
- Тональность комментариев
- Рекомендации AI
- MWS Tables дашборды

#### Social Networks (`/networks`)
- Обзор всех социальных сетей
- Сравнительная статистика
- Динамика по сетям
- Детальная статистика каждой сети
- Фильтрация по сетям

#### Analytics (`/analytics`)
- Детальная аналитика
- Графики динамики
- Сравнение сетей
- Распределение типов
- Тональность комментариев
- Фильтр по сетям и периоду времени

#### Content (`/content`)
- Таблица всех материалов
- Поиск и фильтрация
- Сортировка
- Экспорт в CSV
- Детальная информация о каждом материале

#### ChatBot (`/chat`)
- AI ассистент
- Чат интерфейс
- Примеры вопросов
- История сообщений

### Компоненты

#### ChatInterface
- Интерфейс чата с AI
- Отправка сообщений
- Отображение ответов
- Индикатор загрузки

#### ContentTable
- Таблица контента
- Фильтры и поиск
- Сортировка
- Пагинация

#### MWSDashboard
- Отображение дашбордов из MWS Tables
- Виджеты
- Обновление данных

#### Navigation
- Боковое меню навигации
- Переключение тем
- Активные ссылки

---

## Настройка интеграций

### MWS Tables - Детальное руководство

#### Что такое MWS Tables в этом проекте

MWS Tables - это основной источник данных и система управления контентом. Все данные о публикациях, метриках и аналитике хранятся и управляются через MWS Tables.

#### Архитектура интеграции

```
Социальные сети (VK, Telegram, Instagram, YouTube)
         ↓
    Backend Service
         ↓
    MWS Tables API
         ↓
    Таблица content_registry
         ↓
    Frontend Application
```

#### Структура данных в MWS Tables

**Таблица: `content_registry`**

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | string | Уникальный идентификатор записи |
| `title` | string | Заголовок контента |
| `type` | string | Тип: 'post', 'video', 'image' |
| `date` | datetime | Дата публикации |
| `views` | number | Количество просмотров |
| `likes` | number | Количество лайков |
| `comments` | number | Количество комментариев |
| `reposts` | number | Количество репостов/шаров |
| `network` | string | Социальная сеть: 'vk', 'telegram', 'instagram', 'youtube' |
| `sentiment_positive` | number | Процент позитивных комментариев (0-100) |
| `sentiment_neutral` | number | Процент нейтральных комментариев (0-100) |
| `sentiment_negative` | number | Процент негативных комментариев (0-100) |
| `themes` | array | Массив тем/тегов контента |
| `created_at` | datetime | Дата создания записи |
| `updated_at` | datetime | Дата последнего обновления |

#### Как использовать MWS Tables

**1. Создание и настройка таблицы**

1. Войдите в MWS Tables
2. Создайте новую таблицу `content_registry`
3. Добавьте все необходимые колонки (см. структуру выше)
4. Настройте права доступа

**2. Настройка API подключения**

```env
# В файле .env backend
MWS_API_KEY=your_api_key_here
MWS_APP_ID=your_app_id_here
MWS_BASE_URL=https://api.mws.tables
```

**3. Получение данных в приложении**

Backend автоматически получает данные:
```javascript
// services/mwsService.js
const data = await mwsService.getTableData('content_registry');
```

Frontend запрашивает данные:
```javascript
// services/api.js
const response = await mwsAPI.getContentData();
```

**4. Создание дашбордов**

1. В MWS Tables создайте новый дашборд
2. Добавьте виджеты:
   - **Stats Widget** - для отображения метрик
   - **Chart Widget** - для графиков
   - **Table Widget** - для таблиц данных
3. Дашборд автоматически появится на главной странице приложения

**5. Синхронизация данных из социальных сетей**

Данные автоматически синхронизируются через backend сервисы:
```javascript
// services/socialNetworksService.js
async syncNetworkData(networkId, data) {
  // Данные из социальной сети сохраняются в MWS Tables
  await mwsService.saveToTable('content_registry', data);
}
```

**6. Ручное добавление данных**

Вы можете вручную добавлять записи в MWS Tables:
1. Откройте таблицу `content_registry` в MWS Tables
2. Добавьте новую строку
3. Заполните все поля
4. Данные автоматически появятся в приложении

**7. Обновление данных**

Данные обновляются несколькими способами:
- **Автоматически** - через cron jobs (каждый час)
- **Вручную** - через кнопку "Обновить" в интерфейсе
- **Через API** - POST запросы на `/api/networks/:networkId/sync`

#### Преимущества использования MWS Tables

1. **Единый источник данных** - все данные в одном месте
2. **Визуализация** - встроенные дашборды и графики
3. **Гибкость** - легко добавлять новые поля и метрики
4. **Доступность** - данные доступны через API
5. **Управление** - можно редактировать данные прямо в MWS Tables

### OpenRouter (AI)

1. Зарегистрируйтесь на https://openrouter.ai
2. Получите API ключ
3. Добавьте в `.env`:
   ```env
   OPENROUTER_API_KEY=your_key
   ```
4. AI ассистент будет использовать бесплатную модель Llama 3

### VK API

1. Создайте приложение на https://vk.com/apps?act=manage
2. Получите Service Token
3. Добавьте в `.env`:
   ```env
   VK_SERVICE_TOKEN=your_token
   ```

Подробнее: см. `SOCIAL_MEDIA_APIS.md`

### Telegram, Instagram, YouTube

См. подробную документацию в `SOCIAL_MEDIA_APIS.md`

---

## Разработка

### Структура коммитов

Используйте conventional commits:
- `feat:` - новая функциональность
- `fix:` - исправление багов
- `docs:` - изменения в документации
- `style:` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление задач сборки, настроек и т.д.

### Добавление новой страницы

1. Создайте файл в `content-registry-frontend/src/pages/`
2. Добавьте роут в `App.jsx`
3. Добавьте ссылку в `Navigation.jsx`

### Добавление нового API endpoint

1. Создайте метод в соответствующем service (`services/`)
2. Создайте метод в controller (`controllers/`)
3. Добавьте маршрут в routes (`routes/`)
4. Добавьте метод в frontend API client (`services/api.js`)

### Тестирование

```bash
# Backend тесты
cd content-registry-backend
npm test

# Frontend тесты (если настроены)
cd content-registry-frontend
npm test
```

---

## Решение проблем

### Проблема: AI ассистент не отвечает

**Причины:**
- Не настроен OPENROUTER_API_KEY
- Проблемы с сетью
- Неправильный формат ответа

**Решение:**
1. Проверьте наличие `OPENROUTER_API_KEY` в `.env`
2. Проверьте консоль браузера на ошибки
3. Проверьте логи backend сервера
4. Приложение автоматически использует fallback ответы

### Проблема: Графики не отображаются

**Причины:**
- Не установлен recharts
- Ошибки в данных
- Проблемы с рендерингом

**Решение:**
```bash
cd content-registry-frontend
npm install recharts
```

### Проблема: Таблица контента не загружается

**Причины:**
- Backend не запущен
- Неправильный URL API
- CORS ошибки

**Решение:**
1. Убедитесь, что backend запущен на порту 3000
2. Проверьте прокси настройки в `vite.config.js`
3. Проверьте CORS настройки в backend

### Проблема: MWS Tables не работает

**Причины:**
- Неправильные API ключи
- Недоступен MWS API

**Решение:**
1. Проверьте `MWS_API_KEY` и `MWS_APP_ID` в `.env`
2. Приложение автоматически использует мок-данные при ошибках
3. Проверьте доступность `MWS_BASE_URL`

### Проблема: Цвета отображаются неправильно

**Решение:**
1. Очистите кеш браузера
2. Перезапустите dev сервер
3. Проверьте `index.css` на наличие правильных цветов

---

## FAQ

### Можно ли использовать без реальных API ключей?

Да, приложение автоматически использует мок-данные при отсутствии или ошибках API ключей.

### Какие социальные сети поддерживаются?

- VKontakte (VK)
- Telegram
- Instagram
- YouTube

### Как добавить новую социальную сеть?

1. Добавьте сервис в `services/socialNetworksService.js`
2. Добавьте контроллер в `controllers/socialNetworksController.js`
3. Обновите frontend для отображения новой сети

### Можно ли использовать другую AI модель?

Да, измените модель в `services/aiService.js`:
```javascript
model: "meta-llama/llama-3-8b-instruct:free"
```

### Как изменить цветовую схему?

Измените цвета в `content-registry-frontend/src/styles/index.css`. Текущая схема: красно-бело-светло-серая.

---

## Лицензия

MIT

## Авторы

MWS Smart Nestor Team

---

## Улучшения UX

### Персональные уведомления

Приложение включает систему персональных уведомлений:

- **Уведомления о достижениях** - новые рекорды просмотров, лайков
- **Рекомендации AI** - персональные советы по улучшению контента
- **Предупреждения** - низкая вовлеченность, негативные тренды
- **Обновления** - новые комментарии, изменения метрик

Уведомления отображаются в навигационном меню с индикатором непрочитанных сообщений.

### Дополнительные улучшения UX

- **Фильтрация по социальным сетям** в аналитике
- **Адаптивный дизайн** для всех устройств
- **Быстрая навигация** между разделами
- **Визуальная обратная связь** при действиях
- **Цветовая схема** красно-бело-светло-серая для лучшей читаемости

## Дополнительные ресурсы

- [Документация по API социальных сетей](./SOCIAL_MEDIA_APIS.md)
- [MWS Tables Documentation](https://mws.tables/docs) (если доступна)
- [OpenRouter Documentation](https://openrouter.ai/docs)
- [React Documentation](https://react.dev)
- [Express Documentation](https://expressjs.com)

---

**Последнее обновление:** 2025-11-30 4:05
